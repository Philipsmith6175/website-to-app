import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jamesonsaunders.Gameearningwebsite',
  appName: 'Game Earning Website',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
